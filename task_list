PICKED
  * make changes in log format
    * max size for each record type must be different, in accordance to it's requirement
    * max user record size has to be set big like 80 MB
    * reject user records greater than 80MB, just crash exit(-1) with printf error

  * PAGE_ALLOCATION OPTIMIZATION (almost done)
    * discard debug print statements
    * review new get_extent_id and extent's (using pointer to free_space_mapper_page based) is_full functions
    * find a large enough dataset such that page_allocation is bottleneck and test with and without page_allocation_hints module, so see this module optimizing the logic in real time on a large enough dataset
    * integrate, commit and push

  * in callbacks_bufferpool.h
    * set page alignment to something feasible for both direct io and dma transfer
    * same macro also needed in mini_transaction_engine_checkpointer_util.c